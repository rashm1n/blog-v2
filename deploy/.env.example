# Copy to .env on the VPS (in the same directory as docker-compose.yml), chmod 600, never commit.

DOMAIN=example.com
POSTGRES_PASSWORD=change-me-to-a-long-random-string
APP_SECRET=change-me-to-a-different-long-random-string
